import React from 'react'

const SidebarButtonLabel = ({ label }) => {
    return (
        <span className="mx-2 overflow-hidden text-[--text-color]">
            {label}
        </span>
    )
}

export default SidebarButtonLabel
